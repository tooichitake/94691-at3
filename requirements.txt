# DL AT3 — VizWiz Image Captioning (Phases 1 / 2 / 3)
#
# torch / torchvision: pick the wheel matching your platform.
#   Windows + CUDA 13:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#   Linux + CUDA 12.x:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   macOS / CPU-only:   pip install torch torchvision

torch>=2.3
torchvision>=0.18

# Data + plotting
numpy
pandas
matplotlib
seaborn
pillow
tqdm
pyyaml

# Jupyter
jupyter
ipykernel

# Tokenizer demo (Phase 1 § 7b) + potential Phase 2/3 Transformer encoders (CLIP)
transformers

# Subword tokenization — SentencePiece BPE 8k vocabulary (Phase 1 artifact).
# Kudo & Richardson EMNLP 2018 (arXiv:1808.06226); Google official implementation.
sentencepiece>=0.2.0

# Phase 2/3 evaluation — pure-Python BLEU/CIDEr/ROUGE-L (no Java).
pycocoevalcap
